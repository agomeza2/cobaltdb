# cobaltdb
opensource serverless graphic database manager.  
# Frontend

Para conectar el back con el front se esta utilizando una api rest hecha con node.js en la raiz del documento que se llama server.js  desde alli se toman los datos que tienen alojados en la carpeta y lso tranforma en un unico json para ser enviados al frontend donde se leen y se imprimen en panatalla como un grafo  
